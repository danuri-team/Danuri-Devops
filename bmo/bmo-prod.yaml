apiVersion: secrets.doppler.com/v1alpha1
kind: DopplerSecret
metadata:
  name: bmo-secret
  namespace: doppler-operator-system
spec:
  tokenSecret:
    name: bmo-secret
    namespace: doppler-operator-system
  project: bmo
  config: prd
  managedSecret:
    name: bmo-secret
    namespace: danuri-prod
    type: Opaque
