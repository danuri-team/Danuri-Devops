apiVersion: apps/v1
kind: Deployment
metadata:
  name: danuri-backend
  namespace: danuri
  labels:
    app: danuri-backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: danuri-backend
  template:
    metadata:
      labels:
        app: danuri-backend
    spec:
      containers:
        - name: danuri-backend
          image: ghcr.io/danuri-team/danuri-backend:latest
          ports:
            - containerPort: 8080
          resources:
            limits:
              cpu: 400m
              memory: 1024Mi
            requests:
              cpu: 200m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 20
          envFrom:
            - secretRef:
                name: danuri-image-backend
